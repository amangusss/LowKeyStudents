spring:
  profiles:
    include: datasource
  application:
    name: low-key-students
  datasource:
    url: jdbc:postgresql://localhost:5432/LowKeyStudents
    username: postgres
    password: limon1441
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true